syntax = "proto3";

package Protocol;

// Client -> Server: Login request
message C_LOGIN {
	string id = 1;
	string pw = 2;
}

// Server -> Client: Login response
message S_LOGIN {
	uint32 result  = 1;
	uint64 user_id = 2;
}
